version: 0.2

phases:
  build:
    commands:
      - aws deploy push --application-name Myapp --s3-location s3://saisyustack-mys3buckets-wnkv6ofqku9i/artifact.zip --source src
artifacts:
  files:
    - '**/*'
  base-directory: src